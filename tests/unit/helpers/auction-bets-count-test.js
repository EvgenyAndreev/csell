
import { auctionBetsCount } from 'centersell-frontend/helpers/auction-bets-count';
import { module, test } from 'qunit';

module('Unit | Helper | auction bets count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = auctionBetsCount([42]);
  assert.ok(result);
});

